package com.gffh.api.domain;

/** The four toggles SCR-PR-09 exposes; every {@link NotificationType} belongs to exactly one. */
public enum NotificationCategory {
    FRIENDLY_REQUESTS,
    FIXTURES,
    VERIFICATION,
    MESSAGES
}
